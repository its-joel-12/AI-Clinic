# if needed set up separate virtual env 
python -m venv venv
# and then activate it 
venv\Scripts\activate

# to install required libraries
pip install -r requirements.txt

# to run flask app/Server
python app.py